export const touchScale = {
  default: 0.96,
  compact: 0.9,
} as const

/** TDS interaction dimmer: rgb(0,0,0) opacity on press */
export const PRESS_DIMMER_OPACITY = 0.15

export const PRESS_HOLD_MS = 300
